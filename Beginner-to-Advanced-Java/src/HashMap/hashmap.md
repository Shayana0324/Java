HashMap -- Java's pre-built data structure

-- used wherever data is stored as key-value pairs, where values can be added, retrieved, and deleted using keys.

-- If the hash map does not contain the key used for the search, its get method returns a "null" reference.